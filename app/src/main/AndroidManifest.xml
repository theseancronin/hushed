<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.android.shnellers.hushed">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY"/>
    <uses-permission android:name="android.permission.READ_CONTACTS" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCHX68SB44zJKEsW-gy6S9sw31qAzxT5d8"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".components.MapActivity"/>

        <receiver android:name=".receivers.HushedItemBroadcastReceiver"
                  android:enabled="true">
            <intent-filter>
                <action android:name="com.android.shnellers.hushed.receivers.NEW_DATABASE_ITEM_INSERTED"/>
            </intent-filter>
        </receiver>

        <service android:name=".services.NearbyPlacesSearchService"/>
        <service android:name=".services.GeoFenceTransitionIntentService"/>
    </application>

</manifest>